package y.w.cucumber.stepdef;

import cucumber.api.java8.En;

public class Java8Stepdefs implements En {
    public Java8Stepdefs() {
        Given("^a user is at home$", () -> {
        });
        When("^it is time for lunch$", () -> {
        });
        Then("^the user eats$", () -> {
        });
    }
}
