package y.w.study.alg.array;

/**
 * If, for each pair of states and possible input chars, there is a unique next state
 * (as specificed by the transitions), then the FA is deterministic (DFA).
 *
 */
public class FiniteAutomata
{
//    private int[][] computeTransitionFunction(char[] pattern)
//    {
//        // The transiton function has 1 + pattern.length rows which
//        // represents starting state 0 plus pattern.length states.
//        int[][] tf = new int[pattern.length + 1][pattern.length];
//
//        for (int q = 0; q <= pattern.length; q++)
//            for (char c : pattern)
//            {
//
//            }
//    }
}
